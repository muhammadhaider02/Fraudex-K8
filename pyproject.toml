[project]
name = "fraudex"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "kaggle>=2.0.1",
    "kfp==2.5.0",
    "kfp-kubernetes>=1.0.0",
]
